In this project we learned about vagrant which works as an emulator to connect directly to the virtual machine, we create a reository from our virtual machine we connect by git a version control and remotely host our local changes.